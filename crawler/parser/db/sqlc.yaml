version: "2"
sql:
  - engine: "postgresql"
    schema: "schema.sql"
    queries: "queries/"
    gen:
      go:
        package: "queries"
        out: "gen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
